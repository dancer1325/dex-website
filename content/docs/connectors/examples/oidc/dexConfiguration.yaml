issuer: http://127.0.0.1:5556/dex

storage:
  type: sqlite3
  config:
    #file: var/sqlite/dex.db
    file: /var/dex/dex.db

web:
  #http: 127.0.0.1:5556
  # Reason: run Dex | container
  http: 0.0.0.0:5556

staticClients:
  - id: example-app
    redirectURIs:
      - 'http://127.0.0.1:5555/callback'
    name: 'Example App'
    # REQUIRED this value to work "Login with Example" | built-in client
    secret: ZXhhbXBsZS1hcHAtc2VjcmV0

connectors:
  - type: oidc
    id: google
    name: Google
    config:
      issuer: https://accounts.google.com

      # issuerAlias: https://accounts.google.com

      # $<ENVIRONMENT_VARIABLE>
      clientID: $GOOGLE_CLIENT_ID
      clientSecret: $GOOGLE_CLIENT_SECRET

      # Dex's issuer URL + "/callback"
      redirectURI: http://127.0.0.1:5556/dex/callback

      # basicAuthUnsupported: true

      # TODO: enable groups
      # scopes:
      #  - profile
      #  - email
      #  - groups

      # insecureSkipEmailVerified: true

      # insecureEnableGroups: true

      # allowedGroups:
      #  - <value>

      # TODO: enable it
      # getUserInfo: true

      # userIDKey: nickname

      # userNameKey: nickname

      # acrValues:
      #  - <value>
      #  - <value>

      # promptType: consent

      claimMapping:
      # preferred_username: other_user_name
      # email: mail
      # groups: "cognito:groups"

      claimModifications:
      # _Example of this:_ resulting group == `example::organization::email`
      # newGroupFromClaims:
      #   - prefix: example
      #     delimiter: "::"
      #     clearDelimiter: false
      #     claims:
      #       - organization
      #       - email

      # filterGroupClaims:
      #   groupsFilter: "<REGEX>"

      # _Example:_ if the connector provides a group "regular-users" -> modification converts it -- to -- "example-prefix-regular-usersexample-suffix"
      # modifyGroupNames:
      #   prefix: example-prefix- # note the delimiter at the end
      #   suffix: example-suffix


      overrideClaimMapping: false

      providerDiscoveryOverrides:
      # tokenURL: ""
      # authURL: ""

# if false OR NOT specified -> disable "Login with Email"
# enablePasswordDB: true

#staticPasswords:
#  - email: "admin@example.com"
#    # password  == password
#    hash: "$2a$10$2b2cU8CPhOTaGrs1HRQuAueS7JTT5ZHsHSzYiFPm1leZck7Mc8T4W"
#    username: "admin"
#    name: "Admin User"
#    emailVerified: true
#    preferredUsername: "admin"
#    groups:
#      - "team-a"
#      - "team-a/admins"
#    userID: "08a8684b-db88-4b73-90a9-3cd1661f5466"
